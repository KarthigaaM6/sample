language:node_js
addons:
  sonarcloud:
    organization: "sample-app" # the key of the org you chose at step #3
    token:
      secure: bfaf383b6ab9de68364b2a4325def01c3fe7335e # encrypted value of your token
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner
